//TODO : a faire

!!!!!!!!!BIEN INIT LES STRUCT sinon ca baise tout 


//(déjà préparé avant l’exec) Si c’est un heredoc (R_HEREDOC), 
//tu rediriges l’entrée standard (STDIN_FILENO) vers le fd du heredoc

//reforcer close and free all

//faire attention a appeller cmd quand je parcours avec et pas sehll->list




Builtins dans un pipeline

Les builtins sont exécutés dans le parent uniquement s’il n’y a pas de pipe.
C’est correct, mais attention à bien gérer les effets de bord (cd, export, etc.) dans ce cas.

Gestion du signal lors de l’exec

Tu ignores SIGQUIT dans le shell principal, mais il faudrait le réactiver dans les enfants (processus forkés).





//fix multi heredoc

//normal reachable dans exit et ctrl d ?????



// exemple a gerer : 



//gerer si return d exit = 1 ne pas return, il faut exit contition dans exec built in pas bonne



< Makefile

minishell$ << a << b << c
dup2: Bad file descriptor
bash-5.1$ << a << b << c
>

//faire les redirections et verif si il y a une commande valide apres

//peut etre proteger l appel a find path



//heredoc

bash-5.1$ << $USER
> mfernand
> USR
> $USR
bash-5.1$ << $"USER"
> mfernd
> mfernand
> USR
bash-5.1$ << $'USER'
> USR
bash-5.1$ 


bash-5.1$ << eof cat
> $USER
> eof
mfernand
bash-5.1$ << "eof" cat
> $USER
> eof
$USER
bash-5.1$ << 'eof' cat
> $USER
> eof
$USER
bash-5.1


























👇​👇​👇​👇​👇​👇​👇​
valgrind --leak-check=full --show-leak-kinds=all --trace-children=yes --track-fds=yes --track-origins=yes --log-file=valgrind.log --suppressions=readline.supp ./minishell





Oui, voici d’autres options utiles pour valgrind :

--track-origins=yes
Affiche l’origine des valeurs non initialisées (très utile pour les erreurs "uninitialised value").

--log-file=valgrind.log
Redirige la sortie de valgrind dans un fichier (pratique pour relire les rapports).

--child-silent-after-fork=yes
Réduit le bruit lors de l’utilisation de fork/exec (utile pour les shells).

--show-leak-kinds=all
(Déjà présent) Affiche tous les types de fuites.

--error-exitcode=42
Fait échouer le script si une erreur mémoire est détectée (utile en CI).

